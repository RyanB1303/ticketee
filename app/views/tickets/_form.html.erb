<%= form_with(model: [project, ticket], local: true) do |f| %>
  <% if ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibitted this project from being saved:</h2>
      <ul>
        <% ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <div>
      <%= f.label :name, "Name" %>
      <%= f.text_field :name %>
    </div>

    <div>
      <%= f.label :description, "Description" %>
      <%= f.text_field :description %>
    </div>

    <%= f.submit %>
<% end %>
